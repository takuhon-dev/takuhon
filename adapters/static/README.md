# @ownport/static

Filesystem-backed adapter for ownport — implements `OwnportStorage` on Node.js `fs`.

> **Status**: Phase 4.3 MVP — under active development.

See [`@ownport/core`](../../packages/core) for the storage contract and [`@ownport/cloudflare`](../cloudflare) for the equivalent Cloudflare KV implementation.
