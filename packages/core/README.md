# @meport/core

JSON Schema (`meport.schema.json`), validation, normalization, locale resolution, JSON-LD generation, storage/asset interfaces, and migration registry for meport.

> Phase 1 skeleton. Implementations land in subsequent commits.
