#!/usr/bin/env node

/**
 * create-meport — interactive scaffolding (Phase 3).
 *
 * Will prompt for contentLicense and other initialization options. This is the
 * Phase 1 skeleton; the real prompt flow lands in Phase 3.
 */

export {};
