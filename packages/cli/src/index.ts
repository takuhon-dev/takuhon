#!/usr/bin/env node

/**
 * @takuhon/cli — takuhon CLI (dev/validate/sync/export/migrate/restore).
 *
 * This is the Phase 1 skeleton. Real implementations land in subsequent commits.
 */

export {};
